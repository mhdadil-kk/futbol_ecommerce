<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Page</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f8f8f8;
        }
        .main-container {
            width: 90%;
            max-width: 900px;
            display: flex;
            background: #79b2de;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .form-container {
            width: 50%;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .form-container h2 {
            margin-bottom: 20px;
            font-size: 2rem;
            font-weight: bold;
        }
        .form-container input[type="text"], 
        .form-container input[type="email"], 
        .form-container input[type="password"],
        .form-container input[type="mobile"] {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        .form-container input[type="checkbox"] {
            margin-right: 10px;
        }
        .form-container label {
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }
        .button {
            padding: 10px;
            background-color: #05694e;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }
        .form-container .social-buttons {
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 5px;
            margin-top: 20px;
            width: 50%;
            background-color: #f8f8f8;
            margin-left: 100px;
           
        }
        .form-container .social-buttons img {
            height: 40px;
            cursor: pointer;
        }
        .form-container p {
            margin-top: 20px;
            text-align: center;
        }
        .form-container p a {
            color: #0414e9;
            text-decoration: none;
        }
        .image-section {
            width: 50%;
            background-size: cover;
            background-position: center;
        }
        #go{
            text-decoration: none;
            color: black;
        }
/* 
        @media (max-width: 768px) {
            .main-container {
                flex-direction: column;
            }
            .image-section {
               display: none;
            }
        } */

        .text-danger{
            color: red;
        }
        .alert-success{
            color: greenyellow;
        }
        .alert-error{
            color:red;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="form-container">
            <form class="container" action="/register" method="POST" novalidate>
                <h2>Get Started Now</h2>
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-error"><%= error %></div>
                <% } %>
                <input type="text"  name="name" placeholder="Name" required>
                <% if (errors && errors.some(e => e.path === 'name')) { %>
                    <div class="text-danger">
                       <small> <%= errors.find(e => e.path === 'name').msg %></small>
                    </div>
                <% } %>
                <input type="email" name="email" placeholder="Email address" required>
                <% if (errors && errors.some(e => e.path === 'email')) { %>
                    <div class="text-danger">
                       <small><%= errors.find(e => e.path === 'email').msg %></small> 
                    </div>
                <% } %>
                <input type="mobile" name="mobile" placeholder="Mobile" required>
                <% if (errors && errors.some(e => e.path === 'mobile')) { %>
                    <div class="text-danger">
                       <small><%= errors.find(e => e.path === 'mobile').msg %></small> 
                    </div>
                <% } %>
                <input type="password"  name="password" placeholder="Password" required>
                <% if (errors && errors.some(e => e.path === 'password')) { %>
                    <div class="text-danger">
                       <small><%= errors.find(e => e.path === 'password').msg %></small> 
                    </div>
                <% } %>
                <input type="password"  name="Confirmpassword" placeholder="Confirm password " required>
                <input class="button" type="submit" value="Signup">
                
            </form>
            
            <p>Have an account? <a href="/login">Sign In</a></p>
        </div>
        <div class="image-section" style="background-image: url('assets/imgs/theme/regimg.jpg');"></div>
    </div>
</body>
</html>
